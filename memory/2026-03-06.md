# 2026-03-06

- 방기씨 요청으로 `/home/ppak/Dropbox/Company` 하위 `*.pdf`, `*.hwp`를 RAGFlow에 일괄 업로드 진행.
- 신규 데이터셋 생성: `company-docs-2026-03-06-v2` (`41327c2418e011f1b02bdb21e1fca700`).
- 처리 결과: 총 442개 탐색(PDF 353/HWP 89) → 435개 업로드 성공, HWP 변환 실패 3개, PDF 업로드 실패 4개.
- 작업 로그/매핑 저장: `~/clawd/ragflow-imports/company-docs-2026-03-06-v2/`.
- 파싱 중 임베딩 쿼터 429 에러 1건 확인(일부 문서 RUNNING 상태로 남아있음).
- DB화 마무리를 위해 자동 재시도 루프 스크립트 추가: `~/clawd/scripts/ragflow-retry-loop.sh` (90초 간격, FAIL 문서 20개씩 재파싱 제출), 백그라운드 실행 시작.

## 10:23 삼성전자우 수동 매수 실행
- 요청: "삼성전자우 5주 매수해줘" → 확인 후 즉시 주문 실행.
- 실행: Mac 실서버 `POST /api/trading/buy` 시장가 주문 (`005935`, 5주, `price=0`).
- 결과: 성공, 주문번호 `0016763900`.
- 직후 조회: `005935` 보유수량 `127주`, 당일매수(`thdt_buyqty`) `5주` 반영 확인.

## 10:34 삼성전자 자동매수 미체결 원인 확인
- 질문: "지정 시간에 삼성전자 왜 매수안하지?"
- 서버 로그 확인 결과, 딥바이 사이클에서 **삼성전자 매수 조건 자체는 충족**했지만 자금 안전장치로 매수 차단됨.
  - `삼성전자 매수 조건 충족 → 매도 스킵 (매수 우선)` 반복 기록
  - 동시에 `💰 매수 스킵: 주문가능(1,461,415) < 총자산(22,726,315)의 10%(2,272,631)` 기록
- 결론: 2026-03-05에 추가된 `주문가능금액 < 총자산 10%` 매수 차단 규칙(`buy_blocked_by_cash`)이 원인.

## 10:37 자금 10% 매수차단 규칙 해제
- 방기씨 요청("해제해줘")으로 `simple_deep_buy.py`의 `cash < total_asset * 0.1` 차단 로직 비활성화.
- 변경: `buy_blocked_by_cash = False` 고정 처리.
- 검증/배포:
  - `venv/bin/python3 -m py_compile backend/services/simple_deep_buy.py` 통과
  - backend 재시작 후 `/health` 정상
  - Git 커밋/푸시: `02a7809` (`자금 10퍼 매수차단 해제`)
- 반영 확인: 10:39 사이클에서 더 이상 `매수 스킵: 주문가능...` 로그 없이 자동매수 실행됨 (삼성전자/삼성전자우 당일매수수량 +1 반영).

## 10:40 삼성전자우 직전매수 블록 미동작 원인
- 현상: 10:39 재시작 직후 사이클에서 삼성전자우 1주 추가 매수 발생(직전매수 블록 미동작 체감).
- 로그: `10:39:44`에 `삼성전자우 매수 조건 충족 → ...` 후 `주문 전송: 005935 1주` 확인.
- 원인: `simple_deep_buy.py` 초기화에서 `self._load_last_buy_prices_from_db()`가 비활성화되어 있어, **서버 재시작 시 직전매수가 메모리 맵이 비어 시작**함.
- 결과: 해당 사이클에서 `last_buy_price > 0` 조건이 false라 직전매수 블록이 적용되지 않았고 1주 매수됨.

## 10:53 재확인 - 직전매수 블록 동작 상태
- 로그 기준으로 직전매수 블록은 이후 사이클에서 정상 동작 확인.
  - 10:44/10:45: `삼성전자우 현재가(122,400) ≥ 직전매수가(122,300) → 매수 스킵`
  - 10:46: `현재가(122,500) ≥ 직전매수가(122,300) → 매수 스킵`
  - 10:47: `현재가=122,200`으로 직전매수가(122,300)보다 낮아 조건상 매수 허용 → 1주 매수
  - 10:48/10:51: 직전매수가가 122,200으로 갱신된 뒤 `현재가 ≥ 122,200`에서 다시 스킵
- 10:54 현재 상태: 킬스위치 ON으로 사이클 자체가 `매매 정지 중 - 스킵`됨 (`/api/trading/halt` = `halted:true`).

## 11:06 Heartbeat 점검
- 캘린더 확인: 2시간 이내 일정 없음 (다음 일정 3/10 14:00 주간 미팅).
- Gmail 체크 실패: OAuth 토큰 `invalid_grant` 지속(재인증 필요).
- 주식 백엔드 헬스체크 정상: `{"status":"healthy"}` 응답 확인.

## 11:11 딥바이 카드 대기↔매수 표시 튐(UI) 수정
- 증상: `직전매수 블록` 상태인데도 카드가 간헐적으로 `대기`↔`매수`로 튐.
- 원인: `SimpleDeepBuyCard.tsx`에서 신호 계산 경로가 2개(30초 balance fetch / WebSocket price update)인데 계산식이 서로 달랐음.
  - balance 경로: 직전매수 블록 체크 누락 + `max(평단,매도가)` 기준 사용
  - websocket 경로: 직전매수 블록 체크 포함 + `매도가 우선, 없으면 평단` 기준
- 조치:
  1) `calculateSignal()` 헬퍼 추가하여 두 경로 모두 동일 계산식 사용
  2) 백엔드 로직과 동일하게 통일
     - 매도기록 있으면 매수가 기준=매도가, 없으면 평단가
     - 매도가 기준 갭 0.2% 체크는 **수익 중(current>avg)** 에서만 적용
     - 직전매수가 블록(`current >= last_buy_price`) 공통 적용
  3) 카드 하단 표시(매수 트리거/직전매수 블록) 계산도 동일 조건으로 정렬
- 검증/배포:
  - `npm run build` 성공 (Node v20)
  - frontend 서비스 재기동 후 `/trading` HTTP 200 확인
  - Git 커밋/푸시: `741d2c5` (`딥바이 카드 매수신호 계산식 백엔드와 통일`)

## 14:18 Heartbeat 점검
- 캘린더 확인: 2시간 이내 일정 없음 (다음 일정 3/10 14:00 주간 미팅).
- Gmail 체크 실패 지속: OAuth 토큰 `invalid_grant` (재인증 필요).
- 주식 백엔드 헬스체크 정상: `{"status":"healthy"}` 응답 확인.

## 17:18 Heartbeat 점검
- 캘린더 확인: 2시간 이내 일정 없음 (다음 일정 3/10 14:00 주간 미팅).
- Gmail 체크 실패 지속: OAuth 토큰 `invalid_grant` (재인증 필요).

## 20:18 Heartbeat 점검
- 캘린더 확인: 2시간 이내 일정 없음 (다음 일정 3/10 14:00 주간 미팅).
- Gmail 체크 실패 지속: OAuth 토큰 `invalid_grant` (재인증 필요).

## 21:18 Heartbeat 점검
- 일일 대화 기록 동기화 실행: `~/clawd/clawd-logs/scripts/sync-daily.sh`
- 결과: `2026-03-06.md 복사됨`, `변경사항 없음` (추가 커밋 없음).

## 23:18 Heartbeat 점검
- 일일 대화 기록 동기화 실행: `sync-daily.sh` 결과 변경사항 없음(최신 상태).
- 조용한 시간대(23:00~)로 긴급 알림 사항 없음.
