# 2026-03-09 (Mon)

## 08:20 Heartbeat
- 모닝 브리핑 체크 수행
  - 미국증시(3/6 종가): S&P500 6,740.02(-1.33%), NASDAQ 22,387.68(-1.59%), DOW 47,501.55(-0.95%)
  - 캘린더: 다음 일정은 3/10(화) 14:00 주간 미팅
  - Gmail 체크 실패: `invalid_grant` (토큰 재인증 필요 상태 지속)
  - 날씨: Daejeon ⛅️ -1°C
- 2시간 이내 일정 없음

## 09:13 삼성전자우 50주 수동 매도 실행
- 요청: "삼성전자우 50주 매도해줘."
- 실행: Mac 실서버 `POST /api/trading/sell` 시장가 주문 (`005935`, `삼성전자우`, 50주, `price=0`).
- 결과: 성공, 주문번호 `0009310400`.
- 참고: 첫 호출에서 `stock_name` 누락 검증 에러 확인 후 요청 본문에 종목명 포함해 재실행.

## 09:37 두산에너빌리티 5주 수동 매수 실행
- 요청: "두산에너빌리티 5주 매수해줘."
- 실행: Mac 실서버 `POST /api/trading/buy` 시장가 주문 (`034020`, `두산에너빌리티`, 5주, `price=0`).
- 결과: 성공, 주문번호 `0013237600`.

## 10:23 Heartbeat
- 주식 백엔드 헬스체크: `{"status":"healthy", ...}` 응답 확인 (정상)
- 캘린더 확인: 2시간 이내 일정 없음 (다음 일정: 3/10 14:00 주간 미팅)

## 10:38 수동 점검 요청
- 주식서버 헬스체크 실행: `GET http://127.0.0.1:8000/health` (Mac 내부)
- 결과: HTTP 200, 응답시간 약 0.002s, 상태 `healthy`
- 접속 현황: active_connections=2 / max_connections=500

## 10:39 웹소켓 이상 점검
- 사용자 이슈: "웹소켓이 작동 안하는 것 같고, 서킷브레이크인지" 문의.
- 확인 결과:
  - `GET /api/trading/halt` → `{"halted": false}` (서킷브레이크/강제정지 아님)
  - `/ws` 핸드셰이크 및 수신 정상 (연결 성공 + `price_update` 수신)
  - 백엔드 로그에서 `[WS현황] 총 2개 연결` 확인
  - 다만 실시간 파싱/브로드캐스트 종목이 **`064260` 1종목으로 편중**됨(최근 로그 기준)
- 조치:
  - `POST /api/trading/sync` (`{"portfolio_id":1}`) 실행 → 성공
  - 이후에도 `Parsing Realtime`는 `064260` 위주로 유지 (구독/재구독 로직 추가 점검 필요)

## 10:49 WS 심층 분석
- 06:35 재연결 후 18종목 구독 성공 (SUCCESS 응답 모두 수신)
- 08:30~09:00 프리마켓/시장가에서 18종목 모두 데이터 수신 확인
- 09:00 장개시 직후 대량 데이터 쏟아짐 → 이후 점차 064260만 남음
- 현재(10:49): `tail -n 50` 결과 064260 4건만 수신
- **원인 추정**: KIS WS 서버 측에서 대량 데이터 후 일부 구독이 drop됨 (서버 측 rate limit 또는 버퍼 overflow)
- **해결 방안**: `resubscribe_all()` 호출 필요 → API 엔드포인트 추가 또는 KIS WS 강제 재연결
- 백엔드 재시작하면 `last_buy_prices` 유실 → 재시작 전 반드시 백업 필요

## 10:43 Heartbeat
- 주식 백엔드 헬스체크: `healthy` (active_connections=2)
- 캘린더 확인: 2시간 이내 일정 없음 (다음 일정: 3/10 14:00 주간 미팅)

## 10:52 서버 재시작
- 사용자 결정: KIS WS 이슈 해결 위해 서버 재시작 (last_buy_prices 수동 복원 계획)
- 재시작 완료: PID 13378, health OK
- 직전매수가 수동 복원:
  - 삼성전자(005930): 169,300원
  - 삼성전자우(005935): 116,600원
  - 두산에너빌리티(034020): 93,200원
- WS 구독 18개 성공, 하지만 실시간 데이터는 여전히 064260만 (KIS 서버 측 이슈 추정)
- KIS API 폴링은 정상 작동 → 매매 사이클 문제 없음
- 현재 보유:
  - 삼성전자 45주 (평단 188,607 / 현재 169,300 → -10.2%)
  - 삼성전자우 83주 (평단 137,814 / 현재 116,600 → -15.4%)
  - 두산에너빌리티 13주 (평단 95,388 / 현재 93,300 → -2.2%)
  - 총 평가손실 약 -265만원

### 10:50~11:02 서버 재시작 & WS 재연결 API 추가
- 방기씨 요청으로 백엔드 재시작 (PID 86212 → 13601)
- 직전매수가 수동 복원: 005930=169300, 005935=116600, 034020=93200
- KIS WS 문제 지속: 구독 18개 성공하지만 실시간 데이터 064260만 수신
- **`/api/trading/ws/reconnect` 엔드포인트 신규 추가** (`backend/routers/trading.py` 끝)
  - KIS WS 강제 종료 → approval_key 재발급 → _run_loop 자동 재연결
  - 테스트 성공: 재연결 후 13개 종목 실시간 데이터 수신 확인
  - 앞으로 WS 이슈 시 서버 재시작 없이 이 API로 해결 가능
- 두 번째 재시작 후 직전매수가 다시 설정 완료

## 11:03 서킷브레이커 확인
- **코스피 서킷브레이커 발동**: 10시 31분 52초, 코스피 -8.1% (5,132.07)
- 전 종목 20분간 매매 중단, 코스닥도 매도 사이드카 발동
- **원인: 이란 사태 여파** — 3월 4일 이후 3거래일 만에 또 발동
- WS 데이터 안 온 진짜 원인은 서킷브레이커였음 (매매 중단 → 체결 데이터 없음)
- 서킷브레이커 해제 후 WS 데이터 정상 수신 재개
- 결과적으로 WS reconnect API 추가는 부산물이지만 앞으로 유용할 것

## 12:02 Heartbeat
- 주식 백엔드: healthy (active_connections=2), WS 실시간 9종목 수신 중
- Gmail: invalid_grant 지속
- 캘린더: 2시간 이내 일정 없음 (내일 3/10 14:00 주간미팅)

## 12:02~12:18 수동 매도 직전가 리셋 예외 처리 + 값 정정
- 방기씨 요청: "수동 매도였는데 로직 반영 안됨" → 수동 매도 직전가는 수익률<2% 자동 리셋에서 예외 처리하도록 수정.
- 코드 반영 (Mac `~/개발/stock`):
  - `backend/services/simple_deep_buy.py`
    - `manual_last_sell_codes` 추가
    - 수익률<2% 리셋 로직(fixed/trailing/virtual)에서 수동 보호 종목은 리셋 스킵
    - `mark_manual_last_sell()`/`_set_last_sell_price()` 추가
    - `backend/data/manual_last_sell_codes.json`로 보호목록 영속화
  - `backend/routers/trading.py`
    - `/sell` 성공 시 `mark_manual_last_sell()` 호출
    - `/deep-buy/last-sell-price`도 수동 보호 등록
  - `backend/services/kis_api_service.py`
    - 자동 매도 기록은 `_set_last_sell_price(..., manual=False)`로 처리
- 검증:
  - `/api/trading/deep-buy/last-sell-price?stock_code=005935&price=118200` 등록 후
  - `/api/trading/deep-buy/status`에서 `manual_last_sell_codes=['005935']` 확인
- 배포:
  - 백엔드 재시작 완료, `/health` 정상
  - Git 커밋/푸시: `7bbe613`
- 추가 정정(12:17~12:18):
  - 방기씨 피드백: 두산에너빌리티 직전매수가는 93,200원이 아니라 91,900원
  - `POST /api/trading/deep-buy/last-buy-price?stock_code=034020&price=91900` 재설정 완료
  - `/api/trading/deep-buy/trailing`에서 `last_buy_prices['034020']=91900.0` 확인

## 12:35 직전 매도가 경신 시 1주 분할매도 로직 추가
- 요청: "직전 매도가를 경신하면 한주씩 팔아줘"
- 반영: `backend/services/simple_deep_buy.py`
  - fixed 모드에서 `current_price > last_sell_price`면 수익률(+2%) 조건과 무관하게 1주 매도 실행
  - 매도 시 `last_sell_prices` 갱신되어 같은 가격에서는 재매도 안 되고, 다음 경신 시 다시 1주 매도
  - 알림 문구에 `last_sell_break` 모드 분기 추가 (`📌 직전매도가 경신 매도`)
- 배포: 백엔드 재시작 후 `/health` 정상 확인
- 커밋/푸시: `54d4d69`

## 12:39 직전매수가 재설정
- 사용자 요청: "직전매수가가 다 날라갔어... 다시 설정해줘"
- 보유 종목 확인: 005930(49주), 005935(85주), 034020(20주)
- 재설정:
  - 005930 = 169,300
  - 005935 = 116,200
  - 034020 = 91,900
- 확인: `/api/trading/deep-buy/trailing`의 `last_buy_prices`에 3종목 반영 완료

## 12:40 직전매수/매도가 재시작 보호 영속화
- 요청: "직전매수와 매도가가 서버가 재시작되도 보호되게"
- 코드 반영:
  - `backend/services/simple_deep_buy.py`
    - `backend/data/last_price_state.json` 상태파일 도입
    - 저장 항목: `last_buy_prices`, `last_sell_prices`, `manual_last_sell_codes`
    - 서버 시작 시 상태 자동 로드(`_load_last_price_state`) 후 유지
    - 직전매수/매도가 set/clear 시 상태파일 즉시 갱신
    - startup 중 상태파일 덮어쓰기 방지용 `_state_io_ready` 가드 추가
  - `backend/routers/trading.py`
    - `/deep-buy/last-buy-price`가 `mark_manual_last_buy()` 사용하도록 변경
- 검증:
  1) 값 세팅: 005930=169300, 005935=116200, 034020=91900, 매도가(005935)=118200
  2) 서버 재시작 후에도 동일 값 유지 확인
  3) `manual_last_sell_codes=['005935']` 유지 확인
- 커밋/푸시: `3a843d8`

## 12:52~12:53 수익률 하락 구간 직전매도가 리셋 무효화
- 요청: "수익률 -2%에서 직전매도값 리셋하는거 무효화"
- 반영: `backend/services/simple_deep_buy.py`
  - trailing/fixed/virtual 공통으로 `profit_pct < SELL_RISE_PCT`일 때 직전매도가 자동 리셋하던 로직 제거
  - 이제 수익률 하락(-2% 포함)이어도 `last_sell_prices` 유지
  - 기존 "직전매도가 경신 시 1주 매도" 로직은 유지
- 배포: 백엔드 재시작 후 `/health` 정상 확인
- 커밋/푸시: `81e4ed7`

## 12:21 OpenClaw 업데이트
- 2026.2.17 → 2026.3.7 업데이트 완료
- `npm install -g openclaw@latest --prefix ~/.local`
- 게이트웨이 재시작 완료

## 13:44 삼성전자우 1주 수동 매수 실행
- 요청: "삼성전자우 1주 매수해줘"
- 실행: Mac 실서버 `POST /api/trading/buy` 시장가 주문 (`005935`, `삼성전자우`, 1주, `price=0`).
- 결과: 성공, 주문번호 `0028797900`.

## 13:55 매수 기준을 매도가 전용으로 변경
- 요청: "매수는 무조건 매도가 기준으로 해줘"
- 반영:
  - `backend/services/simple_deep_buy.py`
    - 매수 판단에서 평단가 fallback 제거
    - `last_sell_price`가 없으면 매수하지 않음
    - 적용 위치: 트레일링 경로 + 주기 실행(_run_cycle) 경로 모두
  - `frontend/src/components/trading/SimpleDeepBuyCard.tsx`
    - UI 계산/표시도 동일하게 변경
    - 직전매도가 없으면 `매수 대기(직전매도가 필요)` 표시
- 배포: 백엔드 재시작 후 `/health` 정상
- 커밋/푸시: `c25a835`

## 14:01 삼성전자 매수표시 오탐 정정 (프론트 구버전)
- 증상: "삼성전자 직전매도가 없는데 매수라고 표시"
- 원인: 프론트엔드가 3/6 빌드(next start) 상태로 떠 있어 최신 로직 반영 전 UI가 표시하던 문제.
- 조치:
  - `frontend` 재빌드: `./node_modules/.bin/next build` (PATH에 Cursor helper node 추가)
  - 포트 3000 프로세스 교체 후 `next start` 재기동
- 확인:
  - 백엔드 데이터상 `last_sell_005930=None`
  - 최신 로직 기준 삼성전자 매수는 차단(직전매도가 없으면 매수 불가)

## 14:08 Heartbeat
- 주식 백엔드 체크: `ssh ppak@192.168.0.18 "curl -s http://127.0.0.1:8000/health"` 결과 `healthy` (active_connections=2)
- 캘린더 확인: 다음 일정 `3/10(화) 14:00 주간 미팅` (2시간 이내 일정 없음)
- 조치 필요 이슈 없음 (알림 조건 미충족)

## 14:09 직전매수가 초기화
- 요청: "직전 매수가는 초기화 해줘"
- 실행: 보유 종목(005930/005935/034020) `POST /api/trading/deep-buy/last-buy-price?price=0`
- 결과: `last_buy_prices`가 3종목 모두 `0.0`으로 초기화되어 직전매수 블록 비활성화됨.

## 14:10 삼성전자/두산에너빌리티 각 1주 수동 매도 실행
- 요청: "삼성전자, 두산에너빌리티 1주씩 매도해줘"
- 실행:
  - 삼성전자(005930) 1주 시장가 매도 → 주문번호 `0030333700`
  - 두산에너빌리티(034020) 1주 시장가 매도 → 주문번호 `0030334500`
- 결과: 2건 모두 주문 전송 성공

## 14:23 매도 규칙 단일화 (직전매도 경신 시 1주만)
- 요청: "응 맞춰줘" (자동매도도 직전매도 경신 1주 규칙으로 통일)
- 반영:
  - `backend/services/simple_deep_buy.py`
    - fixed 모드의 기존 `+2% 고정매도` 경로 제거
    - fixed 모드는 `current_price > last_sell_price`일 때만 1주 매도
  - `backend/routers/trading.py`
    - `/deep-buy/trailing`의 fixed 모드 `should_sell/sell_reason` 계산을 새 규칙으로 변경
  - `frontend/src/components/trading/SimpleDeepBuyCard.tsx`
    - SELL 신호/문구를 "직전매도 경신 시 1주" 기준으로 정렬
- 배포:
  - 백엔드 재시작 후 `/health` 정상
  - 프론트 재빌드 및 3000 포트 next-server 재기동
- 커밋/푸시: `4da28de`

## 14:01 표시 이슈 원인 보강
- "삼성전자 직전매도가 없는데 매수 표시"는 프론트 구동 프로세스가 구빌드인 상태(next-server PID 장기 유지) 영향 확인.
- 재빌드/재기동 후 최신 표시 로직 반영.

## 15:08 Heartbeat 정리

### OpenClaw 업데이트
- 2026.2.17 → **2026.3.7** 업데이트 완료
- `npm install -g openclaw@latest --prefix ~/.local`
- 게이트웨이 재시작 완료

### 서킷브레이커 발동 (10:31:52)
- **코스피 -8.1%** (5,132.07) → 전 종목 20분간 매매 중단
- 원인: 이란 사태 여파
- 3월 4일 이후 3거래일 만에 재발동
- 코스닥 매도 사이드카도 발동
- **이게 WS 데이터 안 온 진짜 원인이었음** (매매 중단 = 체결 데이터 없음)

### WS 재연결 API 추가
- `/api/trading/ws/reconnect` 엔드포인트 신규 추가 (`backend/routers/trading.py`)
- KIS WS 강제 끊기 → approval_key 재발급 → _run_loop 자동 재연결 → 재구독
- 앞으로 서버 재시작 없이 WS 문제 해결 가능

### 서버 재시작 2회
- 1차: 방기씨 요청으로 재시작, 직전매수가 수동 복원
- 2차: WS reconnect API 추가 후 재시작, 직전매수가 재복원
- 직전매수가: 삼성전자 169,300 / 삼성전자우 116,600 / 두산에너빌리티 93,200

## 21:57 매수 체결 시 직전매도가 자동 초기화
- 요청: "매수가 이루어지면 직전매도가는 초기화 해줘"
- 반영:
  - `backend/routers/trading.py` (`/buy`)
    - 수동 매수 성공 시 `last_sell_price` 강제 초기화
    - `manual_last_sell_codes`, `sell_block_count` 동시 정리
  - `backend/services/simple_deep_buy.py` (`_execute_buy`)
    - 자동매수 성공/1주 fallback 성공 시 동일하게 직전매도가 초기화
- 배포: 백엔드 재시작 후 `/health` 정상
- 커밋/푸시: `cc475e9`

## 22:16 직전매수가 +3% 자동매도 규칙 추가
- 요청: "직전매수가에서 3% 오르면 매도해주고, 직전매수가는 초기화"
- 반영: `backend/services/simple_deep_buy.py`
  - 매수 미충족 구간에서 `current_price >= last_buy_price * 1.03`이면 1주 자동매도 실행
  - 모드 태그: `last_buy_takeprofit` (알림: `🎯 직전매수가 +3% 매도`)
  - 매도 성공 시 기존 공통 처리로 직전매수가 자동 초기화 유지
- 배포: 백엔드 재시작 후 `/health` 정상
- 커밋/푸시: `a6fe4d2`

## 22:24 직전매도가 복구 (삼성전자/삼성전자우/두산에너빌리티)
- 사용자 피드백: "삼성전자우도 값이 틀리네"
- 로그 기준 최근 직전매도 경신값으로 복구:
  - 005930(삼성전자): 173,500 (14:36 경신)
  - 005935(삼성전자우): 121,200 (15:20 경신)
  - 034020(두산에너빌리티): 96,300 (15:11 경신)
- API 반영: `POST /api/trading/deep-buy/last-sell-price`
- 확인: `last_sell_prices`에 3종목 반영 완료
