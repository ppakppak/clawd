# 2026-03-04

## 08:14 Heartbeat 체크
- 모닝 브리핑 수행 (08:00~10:00)
- 미국 증시 종가(3/3):
  - S&P500 6,816.63 (-0.94%)
  - NASDAQ 22,516.69 (-1.02%)
  - DOW 48,501.27 (-0.83%)
- 캘린더: 예정 일정 없음
- Gmail 체크: `invalid_grant` 오류 지속 (토큰 재인증 필요)
- 날씨 체크: wttr.in 타임아웃으로 실패

## 09:17 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`)
- 캘린더 재확인: 예정 일정 없음

## 09:29 데이터셋 빌드 진행상황 기록
- corrosion dataset 빌드 관련 실행 로그 정리:
  - `neat-fal`: 성공 (`code 0`), 클래스 집계 출력 완료, `TOTAL 11265`
  - `crisp-ri`: 실패 (`code 255`)
  - `neat-tid`: 프로세스 강제종료 (`EXIT_CODE=137`, `python3 /tmp/build_corrosion_dataset.py` 종료)
- 현재 상태: 집계는 확보됐지만 데이터셋 빌드 작업은 미완료 상태
- 결정: 여기까지 저장하고, 다음 세션에서 빌드 재개

## 09:53 주식 자동매수 1회 한도 변경
- 요청: "한번에 매수할 수 있는 한도"를
  - 삼성전자(005930) 2주
  - 삼성전자우(005935) 3주
  로 제한
- 반영 파일:
  - `backend/services/simple_deep_buy.py`
    - `SAMSUNG_MAX_BUY_QTY_PER_ORDER` 추가
    - 매수 실행 직전 종목별 한도 cap 적용
  - `backend/routers/trading.py`
    - 예상 매수수량에도 동일 한도 cap 반영(UI 표시 일치)
- 배포: `main` 브랜치 커밋/푸시 완료 (`e17012d`)
- 적용: 백엔드(uvicorn, :8000) 재시작 후 `/health` 정상 확인

## 11:16 직전매수 블록 강제화
- 이슈: "직전 매수블록인데 왜 매수했지?"
  - 원인 확인: `simple_deep_buy`에서 직전매수 블록이 `buy_ref_label == "평단가"`일 때만 동작
  - `매도가` 기준 매수에서는 직전매수 블록이 우회되어 10:40 사이클 매수 발생
- 요청 반영: **직전매수가 이상이면 무조건 매수 금지**
  - 수정: `backend/services/simple_deep_buy.py`
  - 변경: 기준(평단/매도가)과 무관하게 `current_price >= last_buy_price`면 `should_buy=False`
- 배포: 커밋/푸시 완료 (`82e163a`)
- 적용: 백엔드 재시작 후 `/health` 정상

## 10:57 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`)
- 캘린더 확인: 향후 2시간 내 일정 없음

## 11:18 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`)
- 캘린더 확인: 예정 일정 없음
- Gmail 체크: `invalid_grant` 오류 지속 (메일 조회 불가)

## 11:24~11:44 대시보드 값 고정 이슈 대응
- 증상: 대시보드에서 새로고침해도 보유 종목 가격이 거의 고정으로 보임
- 점검:
  - 백엔드 WebSocket 구독 성공 로그는 정상 확인
  - 브라우저 WS는 재연결 반복(일부 `Replaced by newer connection`) 패턴 확인
  - REST 현재가 조회값(005930/005935)이 반복 조회에서도 동일하게 유지되는 현상 확인
- 코드 수정:
  - `backend/services/realtime_price_service.py`
  - fallback API 가격을 `_price_cache`에 고정 저장하던 로직 제거
  - 목적: 실시간 수신이 끊겼을 때도 새로고침 시 API 재조회로 값 고정 완화
- 배포:
  - 커밋/푸시: `8be04b8` (`fallback API 가격 캐시 고정 제거 (새로고침 시 최신가 재조회)`)
  - KIS WebSocket approval_key 강제 재발급 후 백엔드 재시작
  - 현재 서버 PID `64574`, `/health` 정상, WS 1개 연결 확인

## 11:45 WebSocket 이상 징후 재점검 및 백엔드 재시작
- 사용자 보고: "주식서버 웹소켓이 죽은 것 같다"
- 즉시 점검:
  - `/health` 정상 응답 (`healthy`)
  - `/ws` 핸드셰이크 + ping/pong 정상
  - 다만 실시간 브로드캐스트 로그 부재로 데이터 흐름 정체 의심
- 조치:
  - Mac 원격에서 백엔드(uvicorn :8000) 프로세스 재기동
  - 재기동 후 `backend_run.log`에서 `Parsing Realtime` + `[브로드캐스트] 가격 업데이트 전송` 연속 로그 확인
- 결과:
  - 실시간 시세 수신/브로드캐스트 복구 확인
  - `/health` 정상 유지

## 14:39~14:58 결절/녹 2클래스 데이터셋 빌드 시도 후 일시 중단
- 요청: "일단 결절 녹 2 클래스모델 만들어보자"
- staging(samtel)에서 `/tmp/build_corrosion_dataset_safe.py`로 데이터셋 생성 실행 (PID: `2584129`)
- 중간 진행상태(중단 직전): `train 885`, `val 23`, `test 0` (완료 전)
- 14:58 사용자 요청("잠시 멈추어줘")에 따라 빌드 프로세스 `TERM/KILL`로 중단 완료 (`STOPPED` 확인)
- 현재 상태: 데이터셋 빌드/학습 모두 일시정지

## 15:02~15:15 웹 UI에 데이터셋 빌드 + 학습 탭 실제 구현
- 요청: "지금 하던 과정을 http://192.168.0.32:5005 의 데이터셋 탭과 학습탭에 구현할순 없나"
- 구현 내용:
  1. **GPU API** (`gpu-server/api.py`):
     - `/api/dataset/build_yolo_filtered` 추가 (클래스 필터 + class ID 재매핑)
  2. **백엔드 프록시** (`backend_proxy.py`):
     - `/api/ai/train`, `/api/ai/train/status`, `/api/ai/train/stop`, `/api/ai/models` 프록시 추가
     - `build-multi` → `build_yolo_filtered`로 라우팅 변경
  3. **프론트엔드** (`index.html`):
     - 데이터셋 Step2에 🧪 "부식 2클래스(결절/녹) 프리셋" 버튼 추가
     - 학습 탭 placeholder → 실제 `startTraining()`, `stopTraining()`, 실시간 진행률 구현
     - 빌드 완료 후 "🚀 이 데이터셋으로 학습하기" 버튼 자동 생성
- 커밋: `63dd0c0` on staging main
- staging 서비스 재시작 완료 (5005/5006 정상)
- 후속 수정: `index.html`의 학습 연동 버튼 경로 이스케이프 JS 문법 오류 1건 수정 (`escapedOutput` 라인)

## 16:00 데이터셋 빌드 413 오류 핫픽스
- 증상: 다중 프로젝트(11개/314비디오) 빌드 시 `413 Request Entity Too Large`
- 원인: backend_proxy가 모든 프레임 어노테이션(`annotations_data`)을 JSON으로 GPU 서버에 통째 전송하면서 요청 본문이 100MB 제한 초과
- 조치:
  - `backend_proxy.py`의 `/api/dataset/build-multi`를 **compact payload 모드**로 변경
    - 전송: `projects + classes + output/split + base_projects_dir`만 전달
  - `gpu-server/api.py`의 `/api/dataset/build_yolo_filtered`에 compact 모드 지원 추가
    - GPU 서버가 로컬 프로젝트 디렉토리에서 어노테이션 JSON을 직접 스캔/로드
- 결과: 대용량 전송 경로 제거(413 재발 방지)
- 커밋/푸시: `154b9fe` (pipe-inspector-staging main)

## 11:50 사용자 승인 후 강제 복구 재실행
- 요청: "진행해줘"에 따라 복구 절차 재실행
- 수행:
  1) 기존 백엔드 프로세스 종료
  2) `auth_ws(svr='prod', force_refresh=True)`로 approval_key 강제 재발급
  3) uvicorn 백엔드 재기동 (`backend.main:app`, port 8000)
- 확인:
  - `/health` 응답 정상 (`healthy`)
  - `backend_run.log`에 `실시간 시세 데이터 수신` 다수 확인
  - 실시간 브로드캐스트 동작 확인 (연결된 클라이언트 없을 때는 경고 로그만 출력)

## 11:56 WebSocket 반복 끊김 근본원인 확인 및 수정
- 현상: 약 60~90초 간격으로 WS가 끊기며 `Replaced by newer connection` 반복
- 원인: 서버가 앱 레벨 `{action:"ping"}`를 보내는데 프론트가 `{action:"pong"}` 응답을 안 해서 좀비 연결로 정리됨
- 수정 파일:
  - `frontend/src/app/page.tsx`
  - `frontend/src/hooks/useWebSocket.ts`
- 수정 내용: `onmessage`에서 `action === "ping"` 수신 시 즉시 `pong` 전송
- 배포:
  - 프론트 재빌드/재시작 완료 (`next start`, port 3000)
  - 커밋/푸시 완료: `2b43558` (`fix(ws): respond pong to server ping to prevent periodic disconnect`)
- 검증:
  - 95초 관찰 동안 `Replaced by newer connection` 증가 0건
  - `/health`의 `active_connections=1` 유지

## 14:01 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`, active WS 1)
- 캘린더 확인: 예정 일정 없음
- 메일 체크 주기(3시간)는 아직 미도래(마지막 확인 11:18)

## 13:02 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`, `active_connections=1`)
- 캘린더 확인: 예정 일정 없음
- 메일 체크는 3시간 주기 기준 미도래(마지막 11:18)

## 11:48 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`)
- WebSocket 실시간 수신 로그 재확인: 정상 (`실시간 시세 데이터 수신` 연속 발생)
- 캘린더 확인: 예정 일정 없음

## 12:00 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`)
- WebSocket 연결 상태: 정상 (`active_connections=1`)
- 캘린더 확인: 예정 일정 없음

## 14:47 개발창 무반응 이슈 점검
- 사용자 보고: "개발창 왜 반응을 안하지?"
- 점검 결과:
  - 백엔드(:8000) `healthy`, 실시간 브로드캐스트 정상
  - 프론트(:3000) 리스닝 정상
  - 프론트 로그에 Next.js 런타임 오류 확인:
    - `TypeError: Cannot read properties of null (reading 'digest')`
- 상태: 서버/WS는 정상, 개발창 무반응은 프론트 런타임 오류 가능성 높음

## 14:52 Heartbeat 체크
- 장중 주식 백엔드 헬스체크: 정상 (`healthy`, active WS 1)
- 캘린더 확인: 예정 일정 없음
- Gmail 체크: `invalid_grant` 오류 지속 (메일 조회 불가, 재인증 필요)

## 21:22 Heartbeat 체크
- 캘린더 확인: 예정 일정 없음
- 일일 대화 기록 동기화 수행 (`~/clawd/clawd-logs/scripts/sync-daily.sh`)
  - 결과: `2026-03-04.md` 복사됨
  - Git 변경사항 없음 (추가 커밋 불필요)

## 19:22 Heartbeat 체크
- 캘린더 확인: 예정 일정 없음
- Gmail 체크: `invalid_grant` 오류 동일 (메일 조회 불가, 재인증 필요)
- 변경/신규 알림 사항 없음

## 16:09~16:20 부식 2클래스 대규모 빌드 지연 원인 확인
- 실행: 11개 프로젝트 / 314 비디오 / 클래스 2개(결절, 녹)
- 결과 산출물 생성 완료:
  - `pipe_dataset_20260304_160938`
  - `total_frames=1017`, `train=711`, `val=152`, `test=154`
  - `class_names_yaml=["corrosion_nodule", "corrosion_rust"]`
- 사용자 화면에서 실패로 보인 이유:
  - backend_proxy의 GPU 포워딩 타임아웃이 600초(10분)
  - 실제 빌드는 10분을 약간 초과해 완료됐지만, 10분 시점에 `Request timeout` 반환
- 추가 조치:
  - `backend_proxy.py` `forward_to_gpu()`의 데이터셋 빌드 타임아웃을 3600초(1시간)로 상향
  - backend(:5005) 재기동 후 반영 확인

## 16:24~16:33 야간 프레임 선추출(캐시) 자동화 적용
- 요청: "밤에 일 안하는 시간에 미리 추출" + "그래 해줘"
- 구현:
  1. `gpu-server/api.py` (`/api/dataset/build_yolo_filtered`)
     - **frame cache 우선 모드** 추가
     - 빌드 시 캐시 이미지가 있으면 재사용(hit), 없을 때만 비디오 디코딩(miss)
     - `dataset_info.json`에 cache hit/miss 통계 기록
  2. `gpu-server/warm_frame_cache.py` 신규 추가
     - 전체 프로젝트 어노테이션을 스캔해 annotated frame만 캐시에 선추출
     - 중복/기존 캐시 자동 스킵(증분 워밍업)
  3. 크론 등록 (`intu` crontab)
     - 매일 **01:20** 실행
     - lock(`flock`) + 저우선순위(`nice/ionice`) + 5시간 제한(`timeout`) 적용
     - 로그: `~/projects/pipe-inspector-staging/gpu-server/frame-cache-warm.log`
- 서비스 반영: staging(5005/5006) 재시작 완료
- 커밋/푸시: `90ad1eb` (pipe-inspector-staging main)

## 16:22 Heartbeat 체크
- 장마감 이후(15:30 이후)라 주식 백엔드 장중 헬스체크는 스킵
- 캘린더 확인: 예정 일정 없음
- 메일 체크는 3시간 주기 기준 미도래(마지막 확인 14:52)

## 18:22 Heartbeat 체크
- 장마감 이후(15:30 이후)라 주식 백엔드 장중 헬스체크는 스킵
- 캘린더 확인: 예정 일정 없음
- 메일 체크: Gmail `invalid_grant` 오류 지속(메일 조회 불가, 재인증 필요)

## 19:59 프레임 캐시 워밍업 스케줄 변경
- 요청: "매일 21:20 실행으로 해줘"
- 조치: `intu` 사용자 crontab에서 `warm_frame_cache.py` 실행 시간을 `01:20` → `21:20`으로 변경
- 현재 설정:
  - `20 21 * * * ... warm_frame_cache.py >> frame-cache-warm.log`

## 21:29 samtel 스왑 복구 완료
- 사용자 수동 실행으로 `/etc/fstab` 정리:
  - 잘못된 `/swap.img` 항목 제거
  - `/swapfile none swap sw 0 0` 항목 추가
- 적용:
  - `mkswap -f /swapfile`
  - `swapon /swapfile`
- 확인:
  - `swapon --show` → `/swapfile file 2G`
  - `free -h` → `스왑: 2.0Gi`
  - `systemctl status swapfile.swap` → `active`

## 20:22 Heartbeat 체크
- 캘린더 확인: 예정 일정 없음
- 메일 체크: Gmail `invalid_grant` 오류 지속(메일 조회 불가, 재인증 필요)
- 신규 일정/긴급 알림 없음

## 22:22 Heartbeat 체크
- 캘린더 확인: 예정 일정 없음
- 메일 체크: Gmail `invalid_grant` 오류 지속(메일 조회 불가, 재인증 필요)
- 일일 대화 기록 동기화 재실행 (`~/clawd/clawd-logs/scripts/sync-daily.sh`)
  - 결과: `2026-03-04.md` 복사됨
  - Git 변경사항 없음

## 23:22 Heartbeat 체크
- 조용한 시간(23:00~08:00) 기준으로 긴급 항목만 확인
- 캘린더 확인: 예정 일정 없음
- 긴급 알림 필요사항 없음
