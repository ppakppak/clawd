# 2026-03-05

## Heartbeat
- 08:22 모닝 브리핑 실행
- 미국 증시: S&P500 +0.78%, Nasdaq +1.29%, Dow +0.49%
- 캘린더: 오늘 일정 없음
- Gmail 체크 실패: `invalid_grant` (토큰 재인증 필요, 계속 미해결)
- 날씨(대전): -3°C, 안개

- 12:17 장중 체크: stock backend `/health` 정상(healthy)
- 12:17 캘린더: 일정 없음
- 12:17 Gmail 체크 실패 지속: `invalid_grant` (메일 모니터링 불가)
- 15:46 캘린더: 일정 없음
- 15:46 Gmail 체크 실패 지속: `invalid_grant` (재인증 전까지 메일 체크 불가)
- 16:46 캘린더: 일정 없음
- 대화 메모: 원격 데스크탑 외부 서버는 `wasvr`로 재확인
- 17:46 캘린더: 일정 없음 (2시간 이내 리마인드 대상 없음)
- 대화 메모: 일본 KDDI 연구소/KT Hitel 경력 문구는 `~/SynologyDrive/ykpark/창업대학/script.txt` 138행에서 확인
- 18:31 캘린더: 일정 없음
- 18:31 Gmail 체크 실패 지속: `invalid_grant` (메일 확인 자동화 불가 상태)
- 21:15 일일 대화 기록 동기화 실행: `~/clawd/clawd-logs/scripts/sync-daily.sh` (2026-03-05 복사, git 변경 없음)
- 20:31 캘린더: 일정 없음 (2시간 이내 리마인드 대상 없음)
- 21:19 일일 대화 기록 동기화 재실행: clawd-logs `main`에 커밋/푸시 완료 (`86d2729`)
- 21:19 캘린더: 일정 없음 (2시간 이내 리마인드 대상 없음)
- 21:24 캘린더: 일정 없음 (2시간 이내 리마인드 대상 없음)

## 주식 시스템 대규모 로직 변경 (09:00~21:40)

### 수익률 2% 미만 시 직전매도가 리셋 (커밋 76f1e92)
- fixed/virtual/trailing 전 모드에서 `profit_pct < SELL_RISE_PCT` → `del last_sell_prices[code]`
- 서버 재시작 시에도 유지: `_load_last_sell_prices_from_db()` 후 평단가×1.02 < 매도가면 무효화 (커밋 0e7c54a)

### virtual 매도 모드 비활성화 (커밋 f3d98f8)
- `set_mode("virtual")` 거부, DB 로드 시 virtual→fixed 강제변환

### 매도 성공 시 직전매수가 리셋 (커밋 e4dbee5)
- 전 매도 경로(트레일링/고정/신고가) 6곳에 `del last_buy_prices[code]` 추가
- 직전매수가 DB 로드 비활성화 (커밋 3aae169) — 재시작 시 빈 상태로 시작

### 매매 인터벌 30초 + 수량 1주 고정 (커밋 934d2c4)
- `CHECK_INTERVAL = 0.5` (30초), `SELL_COOLDOWN_SEC = 30`
- `_calc_quantity()` → 항상 return 1
- 스케줄러 루프: `now.minute % interval` 방식 → 단순 `asyncio.sleep(interval*60)`

### 매매 인터벌 드롭다운 UI (커밋들: 1161cf0 → 2eae566 → 89ca666 → c918f5d)
- 백엔드 API: `GET/POST /api/trading/deep-buy/interval`
- 프론트: 30초/1분/2분/3분/5분/10분 드롭다운 선택
- 실시간 카운트다운 타이머 (장외에도 동작)
- `_last_cycle_time` 추적으로 정확한 다음 실행 시각 계산

### 주문가능금액 < 총자산 10% 시 매수 차단 (커밋 f8e933f)
- `buy_blocked_by_cash` 플래그로 매수만 차단, 매도는 유지

### 프론트 빌드 환경 메모
- Mac에 npm/node가 PATH에 없음
- 빌드: `PATH="/Applications/Cursor.app/.../helpers:$PATH" node ./node_modules/.bin/next build`
- 프론트 재시작: 같은 PATH로 `node ./node_modules/.bin/next start -p 3000`
- 또는 nvm 경로: `~/.nvm/versions/node/v25.2.1/bin/node`

### 프론트엔드 에러 (미해결)
- `TypeError: Cannot read properties of null (reading 'digest')`
- `Failed to find Server Action` 반복 — 새 빌드/배포로 해소 가능성

### 백엔드 재시작 주의
- 반드시 `venv/bin/python3` 사용 (시스템 python3는 3.9 → `int | None` 문법 오류)
- 구독 완료까지 ~20초 소요 → health 체크 sleep 25 필요
