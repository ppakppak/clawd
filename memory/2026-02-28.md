# 2026-02-28 (토)

## ToonStyle AI — 이정문 턱 과장 강화 적용 (12:37)
- 요청: "이정문 화백 특징(기이하게 과장된 턱) 가중치 높이기"
- 적용 위치: `~/projects/toon-style-transfer/`

### 변경 사항
1. `scripts/style_transfer.py`
   - `PROMPTS["leejungmoon"]["prompt"]`에 턱 강조 토큰 추가:
     - `dramatically exaggerated jaw`, `protruding chin`, `elongated lower face`, `caricatured angular jawline`
   - `leejungmoon`용 `negative_prompt` 추가:
     - `small chin, v-line jaw, delicate jawline, baby face` 포함
   - CLI 실행 시 화가별 `negative_prompt`를 기본 적용하도록 수정

2. `web/app.py`
   - 기본 프리셋 `이정문 기본` negative/prompt 강화
   - 신규 프리셋 추가: `이정문 턱강화`
     - `faceid_scale=0.30`, `control_strength=0.50`, `guidance=9.0`
   - `/api/transform`에서 `negative_prompt` 미입력 시 화가별 preset 기본값 사용하도록 수정

3. `web/presets.json`
   - `이정문 기본` 프롬프트/네거티브 업데이트
   - `이정문 턱강화` 프리셋 upsert

### 추가 튜닝 (12:56~13:05)
- 사용자 추가 요청: "턱을 더 크게 + 앞으로 튀어나오게", "아직 약함"
- `scripts/style_transfer.py`의 `leejungmoon` 기본 프롬프트를 더 공격적으로 강화:
  - `gigantic lantern jaw`, `pronounced prognathism`, `underbite chin thrust forward`, `jutting mandible`, `hyper-elongated lower face`
- `leejungmoon` 네거티브에 `receding chin`, `flat profile` 추가

- `web/app.py` / `web/presets.json` 프리셋 상향:
  1) `이정문 턱극대화` (기존보다 더 강하게)
     - `faceid_scale=0.14`, `control_strength=0.25`, `guidance=11.5`, `steps=38`
  2) 신규 `이정문 턱실험(초강)`
     - `faceid_scale=0.09`, `control_strength=0.18`, `guidance=12.0`, `steps=42`

### 이정문 v2 재학습 착수 (13:14)
- 사용자 요청: "이정문 v2 재학습 해보자"
- 진행:
  1) `data/processed/*.txt` 51개 캡션 v2 리라이트
     - 스크립트: `scripts/rewrite_captions_leejungmoon_v2.py`
     - 백업: `*.txt.v1.bak`
  2) 학습 설정 신규 생성
     - `configs/train_leejungmoon_v2.toml` (num_repeats=12)
     - `configs/sample_prompts_leejungmoon_v2.txt`
     - `scripts/train_leejungmoon_v2.sh` (output=`leejungmoon_style_v2`)
  3) 웹서버(8501) 종료 후 학습 시작 (GPU 메모리 확보)

- 학습 프로세스: `session=glow-ember`
- 중간 상태: dataset 51장, repeats 12 → train images 612 로드 확인
- 결과: 13:44경 세션 SIGTERM으로 학습 중단
  - 마지막 로그: `4719/6120` steps (~77%), `epoch 8/10`
  - 생성 체크포인트: `leejungmoon_style_v2-000002/000004/000006.safetensors`
  - 최종본(`leejungmoon_style_v2.safetensors`)은 아직 없음

### v2 재개 학습 실행 (14:05) → 완료 (14:22)
- 요청: "재개해줘"
- 재개 방식: 학습 state 저장본이 없어 **epoch6 체크포인트(`leejungmoon_style_v2-000006.safetensors`)에서 이어학습**
- 스크립트 추가: `scripts/train_leejungmoon_v2_resume.sh`
  - `--network_weights=./models/lora/leejungmoon_style_v2-000006.safetensors`
  - `max_train_epochs=4`, `output_name=leejungmoon_style_v2_resume`
- 실행 세션: `quick-bloom`
- 완료 로그: `steps: 100% | 2448/2448`, `model saved`
- 생성 파일:
  - `models/lora/leejungmoon_style_v2_resume-000001.safetensors`
  - `models/lora/leejungmoon_style_v2_resume-000002.safetensors`
  - `models/lora/leejungmoon_style_v2_resume-000003.safetensors`
  - `models/lora/leejungmoon_style_v2_resume.safetensors` (최종)

### v2 모델 서비스 반영 (14:29)
- 사용자 요청: "응 붙여줘"
- 반영 내용:
  - `scripts/style_transfer.py`
    - `PROMPTS["leejungmoon"]`에 버전 `v2-resume` 추가
    - `default_version`을 `v2-resume`으로 변경
  - `web/app.py` 기본 프리셋의 이정문 4종 버전을 `v2-resume`으로 변경
  - `web/presets.json` 이정문 프리셋 4종 버전을 `v2-resume`으로 변경
- 서버 재기동 후 확인:
  - `/api/versions`에서 `leejungmoon.default = v2-resume`
  - `/api/presets`에서 이정문 프리셋 4종 모두 `v2-resume`

### 이정문 화풍확정(얼굴포기) 프리셋 추가 (19:39)
- 사용자 요청: "얼굴 유지 안 해도 좋으니 누구나 이정문 그림으로 알 수 있게"
- 신규 프리셋 추가:
  - 이름: `이정문 화풍확정(얼굴포기)`
  - lora/version: `leejungmoon` / `v2-resume`
  - `faceid_scale=0.03`, `control_strength=0.12`, `guidance=13.0`, `steps=48`
  - 프롬프트: pure black-and-white + very thick contour + gigantic protruding chin + gag manhwa
  - 네거티브: realistic/photo/watercolor/soft shading/color 계열 강제 배제
- 반영 파일:
  - `web/app.py` (DEFAULT_PRESETS)
  - `web/presets.json`
- 서버 재기동 후 `/api/presets`에서 노출 확인
- 샘플 테스트 생성:
  - 입력: `/home/ppak/Documents/카카오톡 받은 파일/before.jpg`
  - 결과: `output/web/toon_leejungmoon_1772275297_9fe8cc.png`

### 젬풍 참고 턱 과장 프리셋 추가 (19:57)
- 사용자 요청: "gem.jpg처럼 턱 과장 가능?"
- 신규 프리셋: `이정문 젬풍 턱블록`
  - `version=v2-resume`
  - `faceid_scale=0.08`, `control_strength=0.32`, `guidance=11.8`, `steps=44`
  - 목표: 블록형 턱 + 펜선 해칭 + 로봇 배경 유지
- 반영 파일:
  - `web/app.py` (DEFAULT_PRESETS)
  - `web/presets.json`
- 샘플 생성 결과:
  - `output/web/toon_leejungmoon_1772276398_542cf3.png`

### JQ 템플릿 모드 확장 (20:20~20:40)
- 사용자 피드백:
  - "템플릿이 얼굴 전체를 대체한 느낌"
  - "정면/반대측면도 대응 필요"
- 기존 단일 토큰(`[JQ001203_TEMPLATE]`)에서 **다중 템플릿 자동 선택**으로 확장

- 추가 템플릿 소스:
  - 정면 기준: `/home/ppak/SynologyDrive/ykpark/wizdata/toons/bw/JQ000034.jpg`
  - 반대측면: `/home/ppak/SynologyDrive/ykpark/wizdata/toons/bw/JQ000062.jpg`
- 생성 파일:
  - `data/templates/jq000034_face_template.png` (640×760)
  - `data/templates/jq000062_face_template.png` (720×780)
  - 기존 `data/templates/jq001203_face_template.png` (측면)

- 코드 변경 (`scripts/style_transfer.py`):
  - 토큰 추가:
    - `[JQ_MULTI_TEMPLATE]` (landmark 기반 자동 방향 선택)
    - `[JQ_TEMPLATE_FRONT]`, `[JQ_TEMPLATE_LEFT]`, `[JQ_TEMPLATE_RIGHT]` (강제 선택)
  - InsightFace 5-point landmark(kps) 읽어서 템플릿 선택 함수 `_select_jq_template()` 추가
  - `transform()` 결과에 `template_file` 메타 반환

- 웹 변경:
  - `web/app.py` / `web/presets.json` 프리셋 3종 추가
    - `이정문 JQ 멀티템플릿(자동방향)`
    - `이정문 JQ 정면 템플릿`
    - `이정문 JQ 반대측면 템플릿`
  - API 응답/메타에 `template_file` 저장

- 확인:
  - `/api/presets`에서 3개 프리셋 노출 확인
  - `before.jpg` + 멀티템플릿 테스트: `template_file=jq001203_face_template.png` 자동 선택

### 턱 워프 방식으로 전환 + 재튜닝 (21:48~22:07)
- 사용자 피드백: "턱이 홀쭉해지고 템플릿과 전혀 다름"
- 조치:
  1) **템플릿 얼굴 치환 방식 중단**
  2) `JQ_CHIN_WARP` 기반 **지오메트리 워프**로 전환
  3) 원형/고리 아티팩트 원인인 강제 턱 라인 리인포스 제거
  4) 워프 함수를 하안부 국소영역만 변형하도록 재작성 (입선 아래 중심)
- 코드 변경:
  - `scripts/style_transfer.py`
    - 토큰 추가: `[JQ_CHIN_WARP]`
    - `_apply_chin_warp()` 재작성 (하안부 볼륨형 팽창, 상안부 보호)
    - chin warp 모드 메타: `chin_warp_mode`, `template_file`
  - `web/app.py`, `web/presets.json`
    - JQ 프리셋 prompt 토큰을 `[JQ_CHIN_WARP]`로 통일
    - 파라미터 재조정(여러 차례): faceid/control/guidance 미세튜닝
- 테스트 결과:
  - `toon_leejungmoon_1772283351_62a07b.png` (1차 전환)
  - `toon_leejungmoon_1772283821_adfe4b.png` (원형 아티팩트 발생)
  - `toon_leejungmoon_1772284279_2bde2d.png` (아티팩트 완화 버전)

### 턱볼륨MAX 추가 실험 (22:20)
- 사용자 요청: "응 해줘" (턱 볼륨 더 강하게)
- 변경:
  - `scripts/style_transfer.py`
    - 토큰 추가: `[JQ_CHIN_WARP_STRONG]`
    - `chin_warp_strength=1.35` 분기 추가
    - 메타에 `chin_warp_strength` 추가
  - `web/app.py` / `web/presets.json`
    - 신규 프리셋: `이정문 JQ 턱볼륨MAX`
    - 파라미터: `faceid=0.03`, `control=0.82`, `guidance=11.6`, `steps=46`
- 테스트 결과:
  - `output/web/toon_leejungmoon_1772285277_62af52.png`
  - 응답 메타: `chin_warp_strength=1.35`

### 2단계 파이프라인(턱고정) 적용 (22:30)
- 사용자 요청: "응 가줘"
- 구현:
  - 토큰 `[JQ_TWO_STAGE]` 추가
  - 2단계 모드에서 워프 후 얼굴 임베딩 재추출 + 스타일 파라미터 완화
  - 중간 산출물 `*_warp.png` 저장
  - API 메타 확장: `two_stage_mode`, `effective_faceid/control/guidance`, `warp_filename`
- 신규 프리셋:
  - `이정문 2단계(턱고정)`
  - prompt: `[JQ_TWO_STAGE] [JQ_CHIN_WARP_STRONG] ...`
- 테스트:
  - 최종: `output/web/toon_leejungmoon_1772285949_d7fb58.png`
  - 워프 중간: `output/web/toon_leejungmoon_1772285949_d7fb58_warp.png`
- 관찰:
  - 파이프라인 동작/기록은 정상이나, 턱 볼륨 증대는 기대 대비 아직 약함

### 서비스 상태
- ToonStyle 웹서버(8501) 재기동 완료
- `uvicorn web.app:app --host 0.0.0.0 --port 8501` LISTEN 확인

### Heartbeat 모니터링 (08:42~20:00)
- 캘린더: 2시간 내 일정 없음(다음 일정: 3/3(화) 14:00 주간 미팅)
- 주식 백엔드: 장중 health OK
- Gmail 체크: 지속적으로 `invalid_grant` 발생(토큰 만료/철회 추정) → 재인증 필요
