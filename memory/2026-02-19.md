# 2026-02-19 (목)

## 🚀 새 프로젝트: Pipe Vision AI
- **목표**: 관로 CCTV 이미지 1장 → AI 자동 결함 분석 리포트 생성
- **위치**: samtel (192.168.0.32) `~/projects/pipe-vision-ai/`
- **단계**:
  1. VLM (GPT-4o/Gemini) + 전문 프롬프트 → 프로토타입
  2. pipe-inspector 라벨링 데이터로 YOLO 학습
  3. YOLO 탐지 + VLM 소견 하이브리드
- **기술 스택**: FastAPI + VLM + 심플 웹 UI
- **상태**: ✅ 프로토타입 구현 완료! 서버 가동 중
  - Python 3.11.14 (pipe-inspector-electron의 .python311_mamba 공유)
  - venv: `~/projects/pipe-vision-ai/.venv/`
  - 포트 5010, URL: http://192.168.0.32:5010
  - Gemini/GPT-4o/Claude 선택 가능
  - API 키: nex 환경변수에서 가져옴 (GEMINI_API_KEY, OPENAI_API_KEY)
  - 구성: main.py + analyzers.py + prompts/ + templates/index.html + start.sh

## 📈 주식 서버 점검 (08:55, 장 개장)
- 백엔드: ✅ healthy (연결 15개, IP 192.168.0.1)
- 보유종목 (7종목):
  - 현대차 4주 (-0.84%)
  - 삼성전자 1주 (-0.98%)
  - 삼성전자우 5주 (+11.47% 🔥)
  - 삼성중공업 32주 (-0.83%)
  - 삼성물산 4주 (-0.74%)
  - 포스코인터 8주 (-1.02%)
  - LG전자 18주 (+0.01%)
- 총자산: 14,009,216원 / 주문가능: 6,399,616원
- 현재가는 아직 어제 종가 (장 막 개장)

## 📊 주식 매매 현황 (09:00~)
- 삼성전자우: 9:20에 **2주 매도** (fixed +1.5% 매도 + 삼성 신고가 매도 동시 발동)
  - 5주 → 3주로 감소
- fixed 매도: 평균가 대비 +1.5% 이상이면 20분마다 체크하여 매도
- 삼성 신고가 매도: 현재가 > last_peak_sell_price이면 1주 추가 매도
- 삼성전자는 1주뿐이라 최소보유(SAMSUNG_MIN_HOLD_QTY=1) 적용으로 매도 안 됨

## 🔧 프론트엔드 WebSocket 통합 수정 (09:33~10:07)
**문제:** 프론트엔드 6곳에서 각각 `new WebSocket()` 생성 → 한 탭에서 5-6개 연결 → IP당 15개 제한 → 순환 끊김/재연결 → 실시간 데이터 멈춤

**수정:** 5개 컴포넌트를 기존 `useWebSocket` 싱글톤 훅으로 교체
- portfolio/[id]/page.tsx
- trading/page.tsx
- TopVolumeStocks.tsx
- SimpleDeepBuyCard.tsx
- HoldingsTable.tsx
- (page.tsx 메인 대시보드는 미수정 — 별도 처리 필요)

**결과:** 519줄 삭제, 201줄 추가, 커밋 `a4ba745`
**효과:** 전체 앱에서 WebSocket 연결 1개만 유지 → 실시간 데이터 멈춤 현상 해결

## 💰 삼성물산 전량 매도 + 블랙리스트 (10:47)
- 삼성물산(028260) 4주 전량 매도 (2주는 이미 자동 매도, 2주 수동 매도)
- `backend/services/blacklist.py` 신규 생성 — JSON 파일 기반 블랙리스트
- `backend/blacklist.json`에 028260 등록
- simple_deep_buy.py + dip_buy_strategy.py 양쪽에 블랙리스트 체크 추가
- 커밋: `2c2daf4`

## ⏰ 낙폭매수 시작 시간 변경 (10:55)
- 12:00 → **11:00** 시작으로 변경
- 커밋: `f2bd449`

## 🔴🔴🔴 심각한 사고: SK텔레콤 과다 매수 (11:00~11:08) 🔴🔴🔴
**사고 내용:**
- 낙폭매수 전략이 SK텔레콤(017670) 매수 시작
- **5주 × 12회 = 60주**, 약 **488만원** 과다 매수!!!
- 11:00부터 11:08까지 매분마다 주문

**근본 원인:**
1. 백엔드 재시작 시 **구 프로세스를 안 죽이고 새 프로세스를 띄움** → 2개 서버 동시 실행
2. **launchd 서비스(`com.ppak.stock-backend`)** 가 자동 재시작시킴 → kill해도 부활
3. 결과적으로 여러 프로세스가 각각 매수 주문을 보냄

**긴급 조치:**
- `kill -9`로 모든 Python 프로세스 강제 종료
- `launchctl unload ~/Library/LaunchAgents/com.ppak.stock-backend.plist` 로 자동 재시작 비활성화
- 현재 백엔드 **완전 정지** 상태

**교훈 (매우 중요!!!):**
- ⚠️ Mac에서 백엔드 재시작할 때 **반드시 launchctl 서비스 확인**
- ⚠️ `ps aux | grep python | grep main.py` 로 **모든** 프로세스 확인 후 kill
- ⚠️ nohup으로 띄우면 launchd와 충돌 → 하나만 써야 함
- ⚠️ 낙폭매수에 동일종목 연속매수 방지, 하루 금액 제한 등 안전장치 필요

**TODO (서버 재시작 전 필수):**
1. 낙폭매수 안전장치 추가 (동일종목 쿨다운, 금액 제한)
2. 프로세스 중복 실행 방지 (PID 파일)
3. 서버 시작 스크립트에 기존 프로세스 kill 포함

## 🔧 기타 수정 커밋
- `0139525` — SimpleDeepBuyCard 문법 오류 수정
- 프론트엔드 `.next` 캐시 삭제 후 dev 재시작 (node v20)
- Mac의 npm/node는 nvm으로 관리 (v18, v20, v25 설치됨, v20 사용)
- targets API에서 SamsungDeepBuyService 캐시 매번 리로드하도록 수정 (미커밋)

## 🔴 SK텔레콤 60주 폭탄 매수 사건 (11:00~11:10)

### 원인 (3중 버그 동시 발생)
1. **헬스체크 크래시 루프**: 서버가 매 1분마다 재시작 (healthcheck가 pkill -9 → 재시작 반복)
2. **`_get_holding_codes()` 버그**: `balance['stocks']` 대신 `balance['holdings']` 써야 하고, `s['code']` 대신 `s['stock_code']` → 보유종목 체크 항상 빈 세트 반환
3. **`reset_daily()` 타이밍**: 11:00~11:10 사이 재시작마다 today_buys 초기화 → 이전 매수 기록 삭제

### 결과
- SK텔레콤(017670) 10번 × 5주 = 50주 + 기존 10주 = **총 60주 (약 488만원)**
- 81,300~81,500원 대에서 계속 매수

### 수정
- `_get_holding_codes()`: `stocks` → `holdings`, `code` → `stock_code` 키 수정
- `reset_daily()`: today_buys에 기록 있으면 초기화 스킵 (재시작 보호)
- 헬스체크 LaunchAgent 일시 중지 (`launchctl unload`)
- 커밋: `976bac4`

### TODO
- 헬스체크 스크립트 개선 (재시작 후 대기시간, 연속 재시작 제한)
- 낙폭매수 전략 활성화 여부 결정

## 📈 오후 주식 매매 현황 (14:00~)
- **14:04** — SK텔레콤 50주 매도 (81,700원) → 아침 폭탄매수 정리
- **14:16** — 삼성전자 2주 매수 (188,400~188,500원) ← 낙폭매수 모듈
- **14:20** — 삼성전자 1주 매도 (188,500원) ← 20분 주기 티어드 익절 (평단 183,000 → +3.0% 충족)
  - 방기씨가 "왜 매도했지?" 문의 → 로그 분석해서 설명함
- 현재: 삼성전자 2주 보유 (평단 186,633원, +1.0%)
- 주식 백엔드: ✅ healthy (연결 1개)

## 14:20~14:39 삼성전자 매도 분석 & 세션 리셋 이슈

### 삼성전자 매매 내역 (오늘)
- 14:16:16 BUY 1주 @ 188,400 (방기씨 요청 → 정아가 수동 매수)
- 14:16:26 BUY 1주 @ 188,500 (방기씨 요청 → 정아가 수동 매수)
- 14:20:04 SELL 1주 @ 188,500 (시스템 자동 - 신고가매도 `_sell_one_share_on_peak`)
- 현재: 2주 보유, 평단 186,633원

### 문제점
1. **수동매수 후 4분 만에 자동매도** - 신고가매도 로직이 수동/자동 구분 없이 매도
2. **세션이 자꾸 리셋됨** - 수동매수 기억을 잃어버림. 원인 미확인
3. 방기씨 지적: 매도가 > 평단가인데 매수 조건은?
   → 코드에 이미 반영됨 (재매수 조건: 182,845원 이하)

### TODO (저녁에 계속)
- [ ] 세션 리셋 원인 파악
- [ ] 수동매수 후 매도 보호 로직 추가 논의
- [ ] 신고가매도 로직 개선 여부 결정

### 매도 로직 정리 (v3.6 simple_deep_buy.py)
- **신고가매도**: 삼성 종목이 last_peak_sell_price 갱신 시 1주 자동 매도 (최소 1주 보유 유지)
- **고정매도**: +1.5% 수익 시 매도
- **매수 조건**: 평균가 -1% 이하 OR 매도가 -3% 이하
- 코드 위치: `~/개발/stock/backend/services/simple_deep_buy.py`

## 19:50~20:13 주식 자동매매 버그 수정 (4건)

### 1. 고정매도 삼성종목 최소보유 체크 버그 (commit a9a7280)
- `_execute_sell_async`에서 `qty`(매도수량)를 보유수량으로 착각
- SAMSUNG_CODES(삼성전자/삼성전자우/현대차) 고정매도가 항상 스킵됨
- 수정: `holdings_cache`에서 실제 보유수량 조회

### 2. 고정매도 기준가 변경 (commit 2c3deef)
- 기존: 평단가 기준 +1.5%
- 변경: `max(평단, 마지막매도가)` 기준 +1.5%

### 3. 매도가 로드 시 정렬 기준 (commit 8288e55)
- `created_at`(DB입력시각) → `transaction_date`(실제 체결일) 기준 정렬
- 동기화로 과거 데이터 일괄 임포트 시 순서 꼬이던 문제 해결

### 4. 매수 기준도 max(평단, 매도가)로 통일 (commit 9604e61)
- 매수/매도 모두 동일한 기준가 사용
- 매수: 기준가 -1%, 매도: 기준가 +1.5%

### 5. sell-signals API 표시도 동일 기준 (commit 97d260b)
- 프론트엔드 표시용 API에서도 매도가 기준 수익률 계산

### 삼성전자우 매도가 수정
- DB에 잘못된 레코드(116,050×40주) 삭제
- last_sell_price를 135,400원으로 수정 (API로 직접 설정)

### 방기씨 요청사항 (미처리)
- 신고가매도 로직에 최소 수익률 조건 추가 필요 (1.5% 미만에서 매도 방지)

## 19:50~20:16 주식 시스템 버그 수정 (4건 커밋)

### 1. 고정매도 삼성종목 최소보유 체크 버그 (a9a7280)
- `_execute_sell_async`에서 `qty`(매도수량)를 보유수량으로 착각
- SAMSUNG_CODES(삼성전자/삼성전자우/현대차) 고정매도가 항상 스킵되던 버그
- 수정: `holdings_cache`에서 실제 보유수량 조회

### 2. 매수/매도 기준가 통일 (2c3deef, 9604e61)
- **원칙**: 기준가 = `max(평단가, 마지막매도가)`
- 매도: 기준가 +1.5% 이상일 때 매도
- 매수: 기준가 -1% 이하일 때 매수
- `_check_fixed_sell`, `_run_cycle` 매수 체크 모두 적용
- 기존 `REBUY_FROM_SELL_PCT(3%)` 별도 로직은 통합 제거

### 3. 매도가 DB 로드 시 transaction_date 기준 정렬 (8288e55)
- `created_at`(DB입력시각) → `transaction_date`(실제 체결일) 변경
- 포트폴리오 동기화로 과거 데이터가 나중에 입력되어도 올바른 순서 유지

### 4. sell-signals API 표시도 동일 기준 적용 (97d260b)
- `samsung_deep_buy_service.py`의 `check_sell_signal`에서도
- `simple_deep_buy.last_sell_prices` 참조하여 매도가 기준 수익률 표시

### 삼성전자우 매도가 수정
- DB에 116,050원(잘못된 일괄임포트) → 135,400원(실제 매도가)으로 수정
- API: `POST /api/trading/deep-buy/last-sell-price?stock_code=005935&price=135400`

### 발견된 추가 이슈
- 신고가매도(`_sell_one_share_on_peak`)가 수익률 조건 없이 실행됨
  → 1.5% 미만에서도 매도 발생 (삼성전자 오늘 4건 전부 +1.0~1.16%)
  → 최소 수익률 조건 추가 필요 (TODO)
- 세션 컴팩션 3회 발생 — 긴 로그 데이터가 원인

### 코드 위치
- 매매 엔진: `~/개발/stock/backend/services/simple_deep_buy.py`
- 표시용 API: `~/개발/stock/backend/services/samsung_deep_buy_service.py`
- `SAMSUNG_CODES = ["005930", "005935", "005380"]`

### 배포 상태 (20:16 기준)
- commit a9a7280, 2c3deef, 8288e55, 9604e61, 97d260b 모두 push 완료
- 백엔드 PID 67835 실행 중이나, **2c3deef 이후 커밋(8288e55~97d260b)은 아직 재시작 안 됨**
- 다음 재시작 시 모든 변경사항 반영됨

### 20:22 프론트엔드 매수/매도 기준가 통일 (commit 0be842c)
- SimpleDeepBuyCard.tsx: 매수/매도 표시를 `max(평단, 매도가)` 기준으로 변경
- 삼성전자가 "평단가"로만 표시되던 문제 해결

### 20:26 백엔드 재시작 (PID 69444 → 69549)
- 모든 커밋(a9a7280~0be842c) 반영

### 20:42 고정매도 기준 원복 (commit 8093979)
- **방기씨 요청**: 고정매도는 평단가 × 1.015만 사용 (매도가 무시)
- 신고가매도(last_peak_sell_price)는 유지
- 프론트엔드 범례도 "매도: 평단가 +1.5%"로 수정
- 백엔드 재시작 완료 (PID 69549)

### 20:57 start.sh 안전장치 추가 (commit 09ac2de)
- 포트 kill + 프로세스명 kill 양쪽으로 기존 백엔드 확실히 종료
- kill 후 프로세스 생존 확인 → 실패 시 시작 중단 (exit 1)

### 현재 매매 로직 정리
```
매수: 현재가 ≤ max(평단, 매도가) × 0.99
고정매도: 현재가 ≥ 평단가 × 1.015
신고가매도: 현재가 > last_peak_sell_price → 1주 (삼성종목만)
```

### 미처리 TODO
- [ ] 신고가매도에 최소 수익률(1.5%) 조건 추가 — 현재 수익률 무관하게 매도됨
- [ ] 수동매수 후 자동매도 방지 로직 논의 (방기씨 언급)

## 20:20~20:46 프론트엔드 매수/매도 기준가 통일 + 고정매도 로직 확정

### 프론트엔드 기준가 통일 (commit 0be842c)
- SimpleDeepBuyCard.tsx: 매수/매도 표시를 `max(평단, 매도가)` 기준으로 통일

### 고정매도 로직 확정 (commit 8093979)
- **방기씨 결정**: 고정매도는 **평단가 기준 +1.5%** (매도가 무시!)
- 신고가매도(현재가 > last_peak_sell_price → 1주)는 유지

### 최종 매매 로직
- 매수: 현재가 ≤ max(평단, 매도가) × 0.99
- 고정매도: 현재가 ≥ 평단가 × 1.015
- 신고가매도: 현재가 > last_peak_sell_price → 1주 (삼성종목만)

### start.sh 안전장치 추가 (commit 09ac2de)
- 기존 프로세스 확실히 kill (포트 + 프로세스명 양쪽)

## 22:51~23:02 Pipe Vision AI 로컬 모델 테스트

### Qwen3-VL 8B 로컬 테스트 (nex, RTX 4090)
- 관로 이미지 분석: **10.3초/장, 비용 0원**
- 한국어 소견 자연스럽고 상세함
- 72B는 단일 24GB GPU에 안 올라감 (4bit=40GB)

### 영상 분석 파이프라인 구상
- 영상 → 키프레임 추출 → YOLO 결함탐지(10ms) → VLM 소견(10초, 결함만) → 리포트
- 30분 영상 → 약 8분이면 전체 소견 완성
