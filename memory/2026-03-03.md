# 2026-03-03

## 08:49 heartbeat
- 모닝 브리핑 체크 실행.
- 미국 증시 지수는 Stooq 기준으로 조회 (Yahoo API 429 rate limit).
- Gmail 체크 스크립트 실패: `invalid_grant` (토큰 재인증 필요 상태 지속).
- 캘린더: 14:00 주간 미팅 1건 확인.
- 날씨: Open-Meteo 기준 대전 현재 3.5°C, 체감 -0.1°C, 약간 흐림(code 2).

## 09:58 제안서 공유
- 방기씨가 KOCCA 사업신청서 PDF 초안 경로 공유:
  `/home/ppak/nas2/Synol_Share/1-1. 사업신청서_(주)인튜웍스_26년_진입형_실증 (1).pdf`
- 현재 작성본 기준으로 내용 점검 요청 맥락.

## 10:00 제안서 빈칸 채움 요청
- 방기씨가 상용화/KPI/일자리/추진일정/사업비 빈칸 채움 초안 작성 요청.
- 심사 문구 톤으로 붙여넣기 가능한 섹션별 초안 제공 예정.

## 12:19 삼성전자우 수동 매수 요청 처리
- 요청: "삼성전자우 2주 매수해줘"
- 실행: Mac 실서버 `POST /api/trading/buy` 시장가 주문 (`005935`, 2주, price=0)
- 결과: 실패 (`HTTP_400`: "주문가능금액을 초과 했습니다")
- 직후 잔고 확인: `orderable_cash=326,933원`, 삼성전자우 현재가 `134,200원` (보유 91주)
- 해석: 시장가(상한가 기준) 2주 주문 시 필요 증거금이 주문가능금액을 초과한 것으로 추정.

## 12:56 heartbeat
- 캘린더 재확인: 14:00 주간 미팅 (2시간 이내) 리마인드 필요.
- 주식 백엔드 헬스체크 정상: `{"status":"healthy"...}` 응답.
- Gmail 체크 여전히 `invalid_grant` (토큰 재인증 필요, 변화 없음).

## 13:40 삼성전자우 10주 수동 매수 실행
- 요청: "삼성전자우 10주 매수해줘"
- 실행: Mac 실서버 `POST /api/trading/buy` 시장가 주문 (`005935`, 10주, price=0)
- 결과: 성공, 주문번호 `0036382300`
- 직후 잔고 반영:
  - 삼성전자우 보유 `101주` (`thdt_buyqty=10`)
  - 주문가능금액 `1,010,950원`

## 13:44 매수 fallback 규칙 합의
- 방기씨 요청: 매수 주문 시 금액 부족으로 실패하면 자동으로 1주만 재주문.
- 적용 범위: 이후 수동 매수 요청 처리 시 기본 fallback으로 적용.

## 13:45 자동매매에도 매수 부족시 1주 fallback 적용
- 방기씨 요청: "아니 자동매매에서도" (매수 금액 부족시 1주 재주문 규칙을 자동매매에도 확장)
- 적용 위치: `~/개발/stock/backend/services/real_account_service.py`
  - `RealAccountService.buy_stock()` 주문 예외 처리에 fallback 추가
  - 조건: 에러 메시지에 `주문가능금액` + `초과` 포함 && 요청 수량 > 1
  - 동작: 동일 주문을 1주로 재시도, 성공 시 `fallback_applied=true`, `requested_quantity` 포함해 success 반환
- 배포:
  - 문법검사: `python3 -m py_compile` 통과
  - backend 재기동 (venv python + uvicorn)
  - `/health` 정상 확인
- Git: `5aba497` (`자동매매 포함 매수금액 부족시 1주 fallback`) main push 완료

## 13:50 heartbeat
- 캘린더: 14:00 주간 미팅 임박(10분 전) 리마인드.
- 주식 백엔드 헬스체크 정상(`status: healthy`).

## 13:57 승강기 프로젝트 점검 요청
- 방기씨 요청으로 Xavier(192.168.0.29) 상태 점검 시도.
- 네트워크: ping 정상(0% packet loss), SSH 포트(22) open, 앱 포트(5000) closed.
- 원격 접속: `ssh ppak@192.168.0.29` 인증 실패(`Permission denied (publickey,password)`)로 프로젝트 내부(git/process/log) 상세 점검은 미수행.
- 외부 웹 확인: `http://192.168.0.29:5000` 및 `/health` 연결 실패.

## 14:01 자동매매 미매수 원인 확인
- 로그 확인: 14:00 딥바이 사이클에서 매수 조건은 충족했으나 주문가능금액 부족으로 미체결.
  - `14:00:06` 삼성전자 4주 매수 시도 → `주문가능금액 초과` 에러
  - `14:00:06` 삼성전자우도 매수조건 충족했으나 `가용 219,759원`으로 계산수량(30주) 자금부족
- 당시 로그 키워드: `[딥바이v3.6] ... 자금 부족`, `매수 오류: 주문 API 오류: 주문가능금액을 초과 했습니다`

## 14:03 자동매매 1주 fallback 실제 반영 (simple_deep_buy)
- 확인 결과: 이전에 적용한 fallback은 `RealAccountService.buy_stock()` 경로만 반영되어, 딥바이 자동매매(`simple_deep_buy`)에는 미적용 상태였음.
- 수정 파일: `~/개발/stock/backend/services/simple_deep_buy.py`
  1) 자금 부족 시 계산수량 대신 1주 fallback 시도 로직 추가
  2) 주문 API에서 `주문가능금액 초과` 예외 발생 시 1주 fallback 재시도 추가
- 배포: py_compile 통과, backend 재기동, `/health` 정상
- Git: `7c0bb52` (`딥바이 자동매매 자금부족시 1주 fallback 적용`) push 완료

## 14:04 승강기 SSH 복구 + 내부 점검
- 방기씨가 `ssh-copy-id -i ~/.ssh/id_ed25519.pub ppak@192.168.0.29` 실행 후, `Number of key(s) added: 1` 확인 (키 설치 완료).
- 확인: `ssh -i ~/.ssh/id_ed25519 ppak@192.168.0.29 "echo ok"` 성공.
- 프로젝트 상태(`~/projects/elevator`):
  - branch: `main`, 최근 커밋: `3dfeec9` (초기 커밋)
  - 워킹트리 변경 없음 (`git status` clean)
  - Python 3.8.10 / venv 존재
  - 포트 5000 리스닝 프로세스 없음, `/health` 접속 실패
- 진단 실행: `timeout 20s python3 main.py --web --use-yolo --source 0 --no-display`
  - YOLO 로딩/워밍업 정상
  - Flask 서버가 `0.0.0.0:5000`에서 정상 기동 확인
  - timeout(20초)로 테스트 종료(code 124) → 상시 실행 프로세스/서비스가 없는 상태로 판단.

## 14:10 승강기 자동실행(systemd user) 설정 완료
- Xavier에 user systemd 서비스 생성: `~/.config/systemd/user/elevator.service`
  - 실행명령: `/home/ppak/projects/elevator/venv/bin/python /home/ppak/projects/elevator/main.py --web --use-yolo --source 0 --no-display`
  - `Restart=always`, 로그 파일: `~/projects/elevator/logs/elevator.out.log`, `elevator.err.log`
- 적용: `systemctl --user daemon-reload && systemctl --user enable --now elevator.service`
- 상태: `is-enabled=enabled`, `is-active=active`
- 부팅 후 자동시작 보장 위해 `loginctl enable-linger ppak` 적용 (`Linger=yes` 확인)
- 접속 확인: `http://192.168.0.29:5000` HTTP 200 응답.

## 14:11 승강기 서비스 재시작/재검증
- 방기씨 요청("응 해줘")에 따라 `elevator.service` 재시작 수행 후 상태 재점검.
- 모델 워밍업 시간 때문에 재시작 직후에는 일시적으로 5000 포트 응답 지연/거부가 발생할 수 있음.
- 최종 상태 확인:
  - `systemctl --user status elevator.service` → active(running)
  - `ss -ltn` → `0.0.0.0:5000` LISTEN
  - 외부 접속 `http://192.168.0.29:5000` → HTTP 200

## 14:12 승강기 프로젝트 진행상황 재점검
- 서비스 런타임:
  - `elevator.service` user systemd 등록/활성화 상태 유지 (`loaded active running`)
  - PID 70142로 `main.py --web --use-yolo --source 0 --no-display` 실행 중
  - 포트 5000 LISTEN 확인
- 웹 접속: `http://192.168.0.29:5000` 응답 코드 200 확인
- 로그 상태:
  - `elevator.out.log`: YOLO 모델 로딩/워밍업 후 서버 시작 메시지 정상
  - `elevator.err.log`: 치명 에러 없음 (개발서버 경고 + 접근 로그만 존재)
- Git 상태: `main` 브랜치, 워킹트리는 `logs/` untracked 외 코드 변경 없음.

## 14:26 네트워크 카메라 연동 적용
- 요청 정보 기반으로 카메라 `192.168.0.72` RTSP 연결 테스트 수행.
- RTSP 포트(554) open 확인.
- 동작 확인된 경로:
  - `/stream1` (주 스트림)
  - `/stream2` (보조 스트림)
  - `/h264/ch1/main/av_stream`, `/h264/ch1/sub/av_stream`
- `elevator.service`의 source를 `--source rtsp://...@192.168.0.72:554/stream1`로 변경 후 재시작.
- 최종 상태:
  - 서비스 active(running)
  - `/stats`에서 frame_count 증가 확인(실시간 프레임 유입 정상)
  - 웹 UI `http://192.168.0.29:5000` 정상 응답.

## 14:29 네트워크 카메라 소스 stream2로 전환
- 방기씨 요청("응 바꿔줘")으로 승강기 서비스 source를 `/stream1` → `/stream2`로 변경.
- 적용 파일: `~/.config/systemd/user/elevator.service` (ExecStart RTSP URL)
- 적용: `systemctl --user daemon-reload && systemctl --user restart elevator.service`
- 최종 상태:
  - 서비스 active(running)
  - `http://192.168.0.29:5000` HTTP 200
  - `/stats` frame_count 증가(실시간 수신 정상)

## 14:34 RTSP 지연(약 30초) 완화 조치
- 증상: 웹 UI 표시 지연이 약 30초 수준으로 큼.
- 원인 추정:
  1) RTSP URL을 비디오 파일로 오인(`is_video_file=True`)하여 재생 지연 로직 적용
  2) 처리 루프 말미 `time.sleep(frame_delay)`로 지연 누적
  3) 캡처 버퍼 설정 미흡
- 코드 수정 (`~/projects/elevator/web_streamer.py`):
  - RTSP/HTTP/UDP 소스는 `is_video_file=False`로 분기
  - 실시간 스트림일 때 `CAP_PROP_BUFFERSIZE=1` 설정
  - 실시간 스트림일 때 sleep 최소화(`0.001s`)
- 서비스 튜닝 (`~/.config/systemd/user/elevator.service`):
  - `OPENCV_FFMPEG_CAPTURE_OPTIONS` 저지연 옵션 추가
  - 실행옵션 튜닝: `--frame-skip 2 --resize-width 480 --jpeg-quality 60`
- 적용/검증:
  - 서비스 재시작 후 active(running)
  - `/stats`에서 `is_video_file=false` 확인
- Git: `39175c8` (`RTSP 저지연: 실시간 스트림 버퍼/슬립 최적화`) push 완료

## 15:20 DeepStream 전환 적용 (1차)
- 방기씨 요청("deepStream 으로 전환")에 따라 `elevator.service` 실행 대상을 OpenCV 웹앱(`main.py`)에서 DeepStream으로 교체.
- 적용 서비스:
  - ExecStart: `/usr/bin/python3 /home/ppak/projects/elevator/deepstream_pose/deepstream_pose_simple.py --source rtsp://...@192.168.0.72:554/stream2 --no-display`
  - 로그: `~/projects/elevator/deepstream_pose/logs/deepstream.out.log`, `deepstream.err.log`
- 상태: `elevator.service` active(running) 확인.
- 참고: DeepStream simple은 `fakesink` 기반이라 기존 웹 UI(`:5000`)는 제공되지 않음.
- 롤백 대비: 이전 OpenCV 서비스 파일을 `~/.config/systemd/user/elevator-opencv.service`로 백업.

## 15:21 DeepStream 앱 점검 메모
- `deepstream_pose_app.py`는 `GstRtspServer` 네임스페이스 의존으로 기본 실행 실패하던 문제 확인.
- `GstRtspServer` import 의존 제거 패치 적용(기본 실행 가능화).
- 하지만 pose app은 `resnet18_baseline_224x224.onnx`/engine 미존재로 엔진 생성 실패 상태.
- 현재는 안정적으로 동작하는 `deepstream_pose_simple.py` 경로 사용 중.
- Git: `cbd40c9` (`deepstream: GstRtspServer 의존성 제거로 기본 실행 가능화`) push 완료

## 15:26 DeepStream 전환 마무리(운영/모니터링 분리)
- 운영 추론: `elevator.service`는 DeepStream(`deepstream_pose_simple.py`) 유지.
- 모니터링 웹: `elevator-preview.service` 신규 추가
  - 실행: `preview_server.py` (RTSP stream2 저지연 미리보기)
  - URL: `http://192.168.0.29:5000`
  - stats: `http://192.168.0.29:5000/stats`
- 결과: DeepStream 추론과 웹 미리보기를 분리해, 기존 5000 웹 확인 동선 복구.
- 리소스(대략): DeepStream ~15% CPU, preview ~7% CPU, RAM 여유 충분.
- 코드 반영:
  - `preview_server.py` 신규 추가
  - `.gitignore`에 `logs/`, `deepstream_pose/logs/` 추가
  - Git: `08f014e` (`운영 마무리: 저지연 미리보기 웹서버 추가 및 로그 경로 ignore`) push 완료

## 15:32~15:40 4채널 구성 마저 세팅
- 방기씨 요청("마저 세팅해줘")에 따라 4채널 운영/미리보기 systemd user 서비스 구성 완료.
- DeepStream 추론 서비스(4):
  - `elevator-ds-webcam.service` (source `0`)
  - `elevator-ds-rtsp.service` (source `rtsp://.../stream2`)
  - `elevator-ds-video1.service` (source `~/projects/elevator/elve1.mp4`)
  - `elevator-ds-video2.service` (source `~/projects/elevator/elve2.mp4`)
- Preview 서비스(4, 포트 분리):
  - `elevator-preview-webcam.service` (`:5000`, source `1`)
  - `elevator-preview-rtsp.service` (`:5001`)
  - `elevator-preview-video1.service` (`:5002`)
  - `elevator-preview-video2.service` (`:5003`)
- 성능 튜닝:
  - `deepstream_pose_simple.py`: RTSP/HTTP를 live-source로 처리, 파일 경로 자동 file:// 변환, no-display 시 `fakesink sync=1`, webcam no-display 처리
  - `preview_server.py`: 숫자 source 카메라 인덱스 처리, 파일 EOF 루프 재생, 파일 소스 fps 제한(15fps)
- 운영 결과:
  - DeepStream 4채널 active
  - Preview `5001/5002/5003` 정상 stats 응답
  - CPU 과부하 상태(90%+)에서 튜닝 후 대략 20~35%대로 안정화
- 제약:
  - webcam preview(`:5000`)은 camera 장치 점유 충돌로 프레임 0일 수 있음(추론 서비스 우선 점유).
- Git: `94b0a95` (`4채널 운영 튜닝: live-source/파일 처리 개선 및 preview 파일 속도 제한`) push 완료

## 15:46 웹캠 미리보기 불가 이슈 처리
- 증상: 방기씨가 `웹캠은 안되네` 보고. `:5000` webcam preview 프레임 0.
- 원인: USB webcam(`/dev/video0`)은 단일 점유 장치라 DeepStream webcam 채널과 preview가 동시 접근 불가.
- 조치:
  - `elevator-ds-webcam.service` 중지/비활성화
  - `elevator-preview-webcam.service`를 source `0`으로 고정 후 재시작
- 결과:
  - `http://192.168.0.29:5000/stats` 정상 (`source=0`, fps 약 9.6, frames 증가)
  - 나머지 채널(`rtsp/video1/video2`)은 DeepStream+preview 모두 active 유지.
- 운영 메모: webcam은 "추론"과 "미리보기" 중 한쪽만 선택해 점유해야 안정 동작.

## 15:55 통합 대시보드(MVP) 구축
- 최종목표(4채널 동시 관제 + 이상 알람) 대응으로 `dashboard_server.py` 신규 구현.
- 기능:
  - 4분할 실시간 화면(5000~5003) 통합 표시
  - 채널별 온라인/FPS/프레임 상태 표시
  - DeepStream 로그 tail 기반 이상 이벤트 수집(쓰러짐/싸움)
  - 실시간 알람 패널(브라우저 비프 + 이벤트 목록)
  - 중복 알람 쿨다운(채널/이벤트별 30초)
  - API: `/api/events`, `/api/health`
  - 옵션: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `ALERT_WEBHOOK_URL` 환경변수 시 외부 알림 전송
- 서비스 배포:
  - `~/.config/systemd/user/elevator-dashboard.service`
  - URL: `http://192.168.0.29:7000`
  - 상태: active(running)
- 검증:
  - `/api/health` OK
  - `/api/events`에서 fall/fight 이벤트 실시간 수집 확인
- Git: `ecb63af` (`관제 대시보드 MVP: 4채널 모니터링 + 이상감지 이벤트 알람 허브`) push 완료

## 19:46 samtel pipe-inspector 프로젝트 점검
- 서버 상태: 정상 운영 중 (backend_proxy.py 2개 프로세스, 포트 5003~5006)
- GPU: RTX 3090 (24GB, 3.2GB 사용), Ollama qwen3-vl:8b 로드됨
- 어노테이션 현황: 총 721건 (jsKim 708건 주력), 8개 프로젝트
- 마지막 커밋: 2026-02-12 (`3cf2d41 데이터셋 탭 수정`) — 약 3주간 코드 변경 없음
- 실사용 확인: dhKim 사용자 18:00 활발히 작업 중 (관내시경 영상 조회/어노테이션)
- wasvr GPU 서버(Tailscale 100.72.127.29:5006) 미응답

## 19:50 K-water 용역 제안서 분석
- 문서: `/home/ppak/nas2/Synol_Share/kwater/최종_정성적평가 제안서.pdf` (50페이지)
- 텍스트 추출: `~/clawd/kwater-proposal.txt` (2,824줄)
- **용역명**: "영상판독 기반의 관상태 평가 기술 개발" (K-water 학술용역)
- **기간**: 2025.08 ~ 2027.12 (3년)
- **구성**: 대표사(인튜웍스, AI/SW 개발) + 공동수급체1(학술/알고리즘) + 공동수급체2(실증/테스트베드)
- **핵심 개발 6대 과업**:
  1. 오토라벨링 솔루션 (속도 33fps, 정확도 95%+)
  2. AI 결함 탐지 모델 - Instance Segmentation (mAP 85%+, P/R 80%+, IoU 70%+)
  3. 결함 크기/면적 산출 - 소실점+Depth Estimation (실측 대비 오차 25% 이내)
  4. 관내부 평면 도식화 - 곡면→평면 전개도 (평면화 오차 20% 이내)
  5. 관상태 평가 알고리즘 - 등급 자동 판정 (기술자 평가와 85%+ 정합성)
  6. 진단 레포트 자동 생성 - PDF (진단시간 80% 단축)
- **연차별 로드맵**:
  - 1차년도: 데이터 수집/전처리/가공 + AI 모델 개발 + 법령조사
  - 2차년도: 평면 도식화 + 크기/면적 산출 + 오토라벨링 UI + 실증
  - 3차년도: 종합 평가 알고리즘 + 레포트 자동화 + UI 완성 + 최종 실증
- **정량 목표**: 실시간 처리 20fps+, 재학습 통한 성능 2%+ 추가 향상
- **현재 갭 분석**:
  - 오토라벨링: 수동 어노테이션만 가능, 자동 미구현
  - AI 모델: qwen3-vl 실험 수준, YOLO/Mask R-CNN 전환 필요
  - 크기 산출/평면 도식화/등급 평가/레포트: 전부 미착수

## 20:10 pipe-inspector 개발 계획 수립
- 방기씨 우선순위 확정:
  1. 데이터셋 빌드 + YOLO 학습 파이프라인 (시제품 10월 마감)
  2. 오토라벨링 고도화 (AI 보조 반자동 라벨링)
  3. 면적 산출 알고리즘 검증
- **1+2를 동시 진행**: YOLO 학습/추론 탭 → 학습된 모델로 반자동 라벨링 루프
- **개발 원칙**: staging(`pipe-inspector-staging`, 5005/5006)에서 개발 → production 배포
- samtel 현황:
  - ultralytics 8.3.246 설치됨, PyTorch 2.6.0+cu124, RTX 3090
  - 최신 데이터셋: `pipe_dataset_20251229_120904` (1,032프레임, 11클래스)
  - 데이터셋 빌드 API 있음, 추론 API 있음
  - **없는 것**: 학습 API, 학습 UI 탭, 반자동 라벨링 연동

## 19:50 K-Water 용역 제안서 분석
- 방기씨가 K-Water 용역 제안서 PDF 공유: `최종_정성적평가 제안서.pdf`
- 과업명: "영상판독 기반의 관상태 평가 기술 개발" (학술용역, 3개년 2025.08~2027.12)
- 전체 분석 및 요약 파일 작성: `~/clawd/memory/kwater-proposal-summary.md`
- pipe-inspector-electron 현재 상태와 제안서 요구사항 매핑 완료
- 핵심 미구현 항목: 크기/면적 산출, 평면도식화, 관상태 평가 등급화, 레포트 자동생성

## 19:57 K-water 1월 월간 공정보고 분석
- 문서: `[인튜웍스]1월 월간 공정보고(AI영상판독).pdf`
- **현재 2차년도** (1차년도 완료됨)
- **누적 공정률**: 계획 3.53% vs 실적 2.40% (달성률 68%) — 공정 부진
- 공정 부진 사유: 데이터 가공 일정 지연
- 완료: 연구사례/법령 조사, 오토라벨링 솔루션 도입 (1차년도)
- 진행중: 결함 크기/식별면적 산출 (픽셀 면적 산출 완료, 검증 필요), 면적비 산출 (3월 완료 예정)
- 미착수: 평면 도식화, 관상태 평가, 레포트, 시범 적용 전부
- 1월 정기회의(1/27): 시제품 10월까지 목표, 테스트 서버에서 데이터셋/학습/추론 기능 구현 중
- 2월 계획: 면적 알고리즘 검증 + 면적비 테스트 + 평면화/정량화 기술 조사 착수

## 21:40 heartbeat
- 일일 대화 기록 동기화 실행: `~/clawd/clawd-logs/scripts/sync-daily.sh`
- 결과: 오늘 메모 파일 복사 확인, 추가 변경사항 없음.
