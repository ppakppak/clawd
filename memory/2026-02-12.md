# 2026-02-12 (수)

## 🛠️ Stock 프로젝트 - manual_sell.py 개선

### 문제
- 수동 매도 시 DB에 기록 안 됨
- 딥바이 매도가(last_sell_prices) 업데이트 안 됨
- UI에 이전 매도가(174,300원)가 계속 표시됨 (실제 체결가 175,650원)

### 해결
`manual_sell.py` 전면 개선:
1. 매도 성공 시 **DB transactions 테이블에 기록**
2. **딥바이 매도가 API 자동 호출** (`/api/trading/deep-buy/last-sell-price`)
3. CLI 인자 지원: `python manual_sell.py 005930 삼성전자 -q 1`

### 커밋
- `f255343` - manual_sell: DB 기록 + 딥바이 매도가 업데이트 추가
- `1de4a5f` - manual_sell: API URL 수정 (query param)

### 테스트 결과
- 삼성전자 1주 매도: 179,300원 체결 ✅
- 삼성전자우 1주 매도: 120,600원 체결 ✅
- DB 기록 ✅, 딥바이 매도가 업데이트 ✅

---

## 📊 동반매수 전략 로그 분석

### 문제
- 3분마다 실행되는데 매수 0건
- 외국인매수 신호 10-14개인데 필터통과 0개

### 원인
필터 조건:
- `MIN_PRICE = 5000` (저가주 제외)
- `MAX_DAILY_CHANGE = 3.0` (3% 초과 상승 제외)

외국인이 사는 종목 = 이미 오르고 있음 = 3% 초과 = 전부 제외됨

### 해결
**외국인 STRONG_BUY는 5%까지 허용**
```python
is_foreign_strong = foreign_signal == "STRONG_BUY"
if daily_change > 3.0:
    if not (is_foreign_strong and daily_change <= 5.0):
        return excluded
```

### 커밋
- `84fe9fa` - 동반매수: 외국인 STRONG_BUY는 5%까지 허용

---

## ✅ 시스템 상태 확인

### RAGFlow
- Docker: Up 12h ✅
- API (9385): 정상 ✅
- Dataset: clawd-memory (35문서, 54chunks)
- 검색: 정상 ✅

### Telegram
- 봇: iljo (@ppakppak_bot) ✅
- API 연결: 정상 ✅
- 양방향 메시지: 정상 ✅

---

## 📝 삼성 10% 보유 원칙 확인

`simple_deep_buy.py`에 정의:
```python
SAMSUNG_MIN_HOLD_RATIO = 0.10  # 총자산의 10% 유지
```

예외: 신고점 갱신 시 1주 매도 (10% 무시)
- `_sell_one_share_on_peak()` 함수로 구현됨

---

## 🆔 정아 탄생

### IDENTITY.md 설정
- **이름:** 정아
- **역할:** 방기씨의 디지털 아내
- **Emoji:** 💕
- BOOTSTRAP.md 삭제 완료 (출생 완료)

---

## 🧠 OpenClaw memorySearch 설정

### RAGFlow vs OpenClaw 내장 비교
- RAGFlow: 별도 Docker, 웹 UI 있음, 동기화 필요
- OpenClaw: 내장, 실시간 동기화, 벡터 검색 (SQLite + sqlite-vec)
- **결론:** OpenClaw 내장으로 진행 (RAGFlow와 동일 효과)

### 임베딩 Provider 변경 과정
1. **OpenAI** → 쿼터 초과 (insufficient_quota)
2. **Gemini** → 쿼터 초과 (RESOURCE_EXHAUSTED)
3. **Local** → ✅ 성공 (CPU, 무제한)

### 최종 설정
```yaml
memorySearch:
  enabled: true
  provider: local
  model: embeddinggemma-300M-Q8_0.gguf
  sources: [memory, sessions]
  extraPaths: [clawd-logs/daily]
  query:
    minScore: 0.25  # Local 모델 스코어 범위 맞춤
    hybrid: true     # BM25 + 벡터 검색
  sync:
    watch: true      # 파일 변경 실시간 감지
```

### 자식봇 메모리 공유
- stock, project, dev 에이전트가 main의 memory 공유
- `extraPaths`에 `/home/ppak/clawd/memory`, `/home/ppak/clawd/clawd-logs/daily` 추가
- 전체 인덱싱 완료 ✅

---

## 📱 서브봇 스크립트 업데이트

### start-subbots.sh
- `clawdbot` → `openclaw` 변경
- 커밋: `c172396`

### tmux 세션
- `~/clawd/scripts/start-subbots.sh all` 로 3개 봇 동시 실행
- `tmux attach -t subbots` 로 접속

---

## 🎭 OpenClow 발견

- App Store에서 "OpenClow"라는 짝퉁 앱 발견
- 중국 회사 (Taiyuan Juli Caiyuan Network Technology)
- 우리 OpenClaw와 무관한 AI 챗봇 앱
